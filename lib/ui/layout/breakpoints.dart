/// Namespace for Device Breakpoints
abstract class DeviceBreakpoints {
  /// Max width for a small layout.
  /// Screen size of a phone
  static const double small = 600;

  /// Max width for a medium layout.
  /// Screen size of a tablet
  static const double medium = 1280;

  /// Max width for a large layout.
  /// Screen size of a desktop
  static const double large = 1920;
}
