import 'package:flutter/material.dart';

class FriendsTile extends StatelessWidget {
  const FriendsTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
